====================================================================
Day 8 — 한국어 문서 요약 서비스 통합 테스트
====================================================================

====================================================================
[테스트 1] 서버 / 모델 상태 확인
====================================================================
   응답: {'status': 'ok', 'model_loaded': True, 'model_name': 'digit82/kobart-summarization', 'device': 'cpu', 'uptime_sec': 8.8}
   [PASS] GET /health 가 200을 반환 — status=200
   [PASS] 모델이 메모리에 로드됨
   [PASS] 인증 없이도 호출 가능(모니터링 용도)

====================================================================
[테스트 2] API Key 인증
====================================================================
   키 없음        → 401 {'success': False, 'error': 'API Key가 필요합니다. X-API-Key 헤더를 포함해 주세요.'}
   [PASS] 키 없이 요청하면 401 — status=401
   잘못된 키      → 401 {'success': False, 'error': '유효하지 않은 API Key입니다.'}
   [PASS] 등록되지 않은 키면 401 — status=401
   정상 키        → 200
   [PASS] 유효한 키면 200 — status=200
   [PASS] 응답에 인증된 사용자가 담김 — requested_by=사용자A

====================================================================
[테스트 3] 입력 검증 (Pydantic)
====================================================================
   text 누락                  → 422 ['text: 필수 항목입니다.']
   [PASS] text 누락 → 422 — status=422
   30자 미만 텍스트               → 422 ['text: 30자 이상이어야 합니다.']
   [PASS] 30자 미만 텍스트 → 422 — status=422
   공백만 100자                 → 422 ['(본문): text가 공백 문자로만 이루어져 있습니다.']
   [PASS] 공백만 100자 → 422 — status=422
   8000자 초과                 → 422 ['text: 8000자 이하여야 합니다.']
   [PASS] 8000자 초과 → 422 — status=422
   min_length >= max_length → 422 ['(본문): min_length(60)는 max_length(30)보다 작아야 합니다.']
   [PASS] min_length >= max_length → 422 — status=422
   num_beams 범위 초과          → 422 ['num_beams: 8 이하여야 합니다.']
   [PASS] num_beams 범위 초과 → 422 — status=422
   max_length 타입 오류         → 422 ['max_length: 정수여야 합니다.']
   [PASS] max_length 타입 오류 → 422 — status=422

====================================================================
[테스트 4] 요약 결과와 옵션 반영
====================================================================
   원문(326자): 정부는 24일 국무회의를 열고 인공지능 산업 육성을 위한 종합 대책을 확정했다. 이번 대책...
   요약(99자): 정부는 24일 국무회의를 열고 국산 AI 반도체 개발 지원, 데이터센터 전력 인프라 확충, AI 인재 10만 명 양성 계획이 포함된 AI 산업 육성을 위한 종합 대책을 확정했다.
   압축률 30.4% · 5068ms · 입력 110토큰 · 덩어리 1개
   [PASS] 요약문이 비어 있지 않음
   [PASS] 요약문이 원문보다 짧음 — 99 < 326
   [PASS] 요약이 문장으로 끝남 (중간에서 끊기지 않음) — 끝 문자 '.'
   [PASS] 한 번에 처리됨 (덩어리 1개) — chunks=1
   max_length=20 요약(61자): 정부는 24일 국무회의를 열고 국산 AI 반도체 개발 지원, 데이터센터 전력 인프라 확충, AI 인재 10만…
   문장 단위 정리 여부: True
   [PASS] max_length를 줄이면 요약이 더 짧아짐 — 61 < 99
   [PASS] 짧게 잘린 요약도 문장 끝으로 정리됨 — 끝 문자 '…'

====================================================================
[테스트 5] 동시 요청 (run_in_executor)
====================================================================
   단일 요청 1건: 4.81초
   동시 요청 4건: 28.35초 (상태 코드 [200, 200, 200, 200])
   순차 처리 예상치 19.23초 → 실제 28.35초
   (CPU 추론이라 처리량 자체는 코어 수에 묶인다. 스레드 풀의 진짜 이득은 아래 이벤트 루프 응답성이다.)
   [PASS] 동시 요청 4건이 모두 200 (실패·타임아웃 없음) — [200, 200, 200, 200]
   추론이 도는 중 /health 응답: 200 (12ms)
   [PASS] 무거운 추론 중에도 이벤트 루프가 막히지 않음 — 12ms

====================================================================
[테스트 6] 한 번에 처리할 분량을 넘는 긴 문서 (분할 요약)
====================================================================
   원문 2490자
   [PASS] 긴 문서도 200으로 처리됨 — status=200
   1021토큰 → 덩어리 2개 · 요약 187자 · 22411ms
   요약: 머신러닝 모델을 실험실에서 서비스로 옮기는 과정은 학습 단계에서는 정확도가 유일한 목표지만, 서비스 단계에서는 응답 시간과 안정성, 비용이 함께 평가된다. 한 번 넘는 문서를 그대로 밀어 넣으면 뒷부분은 조용히 버려지고, 사용자는 앞부분만 반영된 결과를 받는 서비스라면, 인증 없이 열린 추론 엔드포인트는 그 자체로 공격 표면이 된다.
   [PASS] 덩어리 한도(900토큰)를 넘어 여러 덩어리로 나뉨 — 1021토큰 → 2덩어리
   [PASS] 나눠 요약해도 원문보다 훨씬 짧음 — 압축률 0.075
   [PASS] 덩어리별 요약이 살아 있어 지나치게 압축되지 않음 — 187자 / 2덩어리
   [PASS] 결과가 문장으로 끝남 — 끝 문자 '.'

====================================================================
결과: 27건 성공 / 0건 실패 (총 27건)
====================================================================
